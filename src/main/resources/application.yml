server:
  port: 9100

app-config:
  oauth:
    jwt-secret: 7b615e6dc7682359c47cd11ff64b8f23

  cep:
    url: https://viacep.com.br/ws/%d/json/

spring:
  application:
    name: gestao-mercados-api

  flyway:
    enabled: true
    out-of-order: true
    baseline-on-migrate: true

  jpa:
    database: h2
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: